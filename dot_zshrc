unsetopt autocd

if type direnv >/dev/null 2>&1; then
	eval "$(direnv hook zsh)"
fi

# Run other ZSH scripts
for file in `find "$HOME/.zshrc.d" -type f -name "*.sh" -maxdepth 1`; do
    source $file
done