#!/usr/bin/env bash

URL=$1

if [ -z "$URL" ]; then
    echo "Usage: vimstall [url]" 1>&2
    exit 1
fi

NAME=$(basename $URL)

pushd $ZSH >/dev/null
git submodule add $URL vim.symlink/bundle/$NAME
popd >/dev/null
