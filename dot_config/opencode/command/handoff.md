---
description: End-of-session capture — land everything durable into beads and brain
---

Wrap up this session by making the conversation disposable:

1. For each bead touched: close it (with what/how-verified notes) if done,
   otherwise update it with exact resume state — done, remaining, dead ends.
2. `bd create` a bead for every follow-up, idea, or TODO mentioned in this
   conversation that isn't already tracked. List what you created.
3. `bd remember` any one-paragraph operational gotchas learned today.
4. If anything discussed changes how a system works or why, update
   `$TOWER_BRAIN/doc/` or append to `$TOWER_BRAIN/brain/DECISIONS.md`.
5. Confirm: "state captured — this conversation can be discarded", or list
   what still lives only in chat and why.
